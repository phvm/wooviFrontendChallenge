import { styled } from "@mui/material";
import { Typography } from "@mui/material";

export const CashbackPercent = styled(Typography)({
  color: "#03d69d",
  fontSize: "0.75rem",
});

export const CashbackFlag = styled(Typography)({
  color: "#ffffff",
});
