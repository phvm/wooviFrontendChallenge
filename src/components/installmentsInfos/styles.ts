import { styled, Typography } from "@mui/material";

export const InterestPercentage = styled(Typography)({
  color: "#ffffff",
});

export const TotalAmout = styled(Typography)({
  color: "#AFAFAF",
  fontSize: ".8rem",
  fontWeight: "500",
});

export const InstallmentNumber = styled("b")({
  fontWeight: "700",
  color: "#4D4D4D",
});
