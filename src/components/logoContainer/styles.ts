import { styled } from "@mui/material";

export const Container = styled("header")({
  width: "100dvw",
  margin: "2rem 0",
});

export const Logo = styled("img")({
  margin: "auto",
  display: "block",
});
