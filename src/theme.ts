import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    allVariants: { color: "#4D4D4D" },
    fontFamily: ["Nunito", "sans-serif"].join(","),
  },
});

export default theme;
